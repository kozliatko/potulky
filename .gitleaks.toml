title = "Potulky — gitleaks konfigurácia"

[extend]
useDefault = true

[allowlist]
description = "Placeholdery a testovacie fixtures, nie sú to reálne secrets"
paths = [
  '''\.env\.example$''',
  '''backend/server\.test\.js$''',
]
